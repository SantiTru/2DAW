let arreglo_modulos = ["Dam", "Daw", "Asir"];

arreglo_modulos.forEach((elemento, pos) => {
  console.log(elemento);
  console.log(pos);
});

arreglo_modulos.forEach((elto) => console.log(elto));

arreglo_modulos.forEach(elto => console.log(elto));
